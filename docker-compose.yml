version: '1.0.0'

volumes:
        node_data: {}
        certbot-etc:
        certbot-var:
        web-root:
          driver: local
          driver_opts:
            type: none
            o: bind
            device: "var/www/html"


services:

  certbot:
    container_name: certbot
    image: certbot/certbot:latest
    depends_on:
      - webserver
    volumes:
    - ./certbot/conf:/etc/letsencrypt
    - ./certbot/www:/var/www/html
    command: certonly --webroot -w /var/www/certbot --force-renewal --email owencoogan01@gmail.com -d crece-en-libertad.owensapi.com --agree-tos
  db:
    container_name: pg_container
    image: postgres

    ports:
      - "5432:5432"

    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bookazikos_production

    restart: unless-stopped
    volumes:
        - node_data:/var/lib/postgresql/data


  webserver:
    image: nginx:mainline-alpine
    container_name: webserver
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - node_data:/var/www/html
      - ./nginx-conf:/etc/nginx/conf.d
      - certbot-etc:/etc/letsencrypt
      - certbot-var:/var/lib/letsencrypt
      - ./certbot/www:/var/www/certbot
    depends_on:
      - bookazikos-node-js


  bookazikos-node-js:
    image: bookazikos-node-js
    environment:
      PG_USER: admin
      PG_PASSWORD: postgres
      PG_HOST: db
    depends_on:
        - db
    ports:
        - "6950:6950"
    volumes:
          - node_data:/var/lib/postgresql/data
    command: [ "npm","run","prep-db-prod"]


